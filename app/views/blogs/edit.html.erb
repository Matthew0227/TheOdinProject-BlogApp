<% content_for :title, "Editing blog" %>

<div class="mx-auto max-w-2xl rounded-xl border border-gray-200 bg-white p-6 shadow-sm">
  <h1 class="mb-6 text-2xl font-semibold text-gray-900">Editing blog</h1>

  <%= render "form", blog: @blog %>

  <div class="mt-6 flex flex-wrap gap-3">
    <%= link_to "Show this blog", @blog, class: "text-sm font-medium text-blue-600 hover:text-blue-700" %>
    <%= link_to "Back to blogs", blogs_path, class: "text-sm font-medium text-gray-600 hover:text-gray-800" %>
  </div>
</div>
